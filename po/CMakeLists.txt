# Translations
include (Translations)
add_translations_directory ("super-wingpanel")
add_translations_catalog ("super-wingpanel"
    ../src/Widgets
    ../src/Services
    ../src/Drawing
    ../src/Indicator
    ../src
)
